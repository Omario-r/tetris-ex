import 'package:flutter/material.dart';

/// A placeholder widget for the game loop.
///
/// This is a stub that will be implemented later.
/// The actual game loop logic uses [AnimationController] to drive
/// the game at approximately 60fps.
class GameLoop extends StatelessWidget {
  /// Creates a [GameLoop] widget.
  const GameLoop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
